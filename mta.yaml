_schema-version: "3.2"
ID: workbench
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: workbench-destination-content
  type: com.sap.application.content
  requires:
  - name: workbench-destination-service
    parameters:
      content-target: true
  - name: workbench-repo-host
    parameters:
      service-key:
        name: workbench-repo-host-key
  - name: workbench-uaa
    parameters:
      service-key:
        name: workbench-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: workbench_html_repo_host
          ServiceInstanceName: workbench-html5-srv
          ServiceKeyName: workbench-repo-host-key
          sap.cloud.service: workbench
        - Authentication: OAuth2UserTokenExchange
          Name: workbench_uaa
          ServiceInstanceName: workbench-xsuaa-srv
          ServiceKeyName: workbench-uaa-key
          sap.cloud.service: workbench
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: workbench-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: workbench-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - workbench.zip
      name: workbench
      target-path: resources/
- name: workbench
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: workbench-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: workbench-destination-service
    service-plan: lite
- name: workbench-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: workbench-xsuaa-srv
    service-plan: application
- name: workbench-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: workbench-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
